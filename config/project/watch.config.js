export const config = {
  /**
   * Timeout (in milliseconds) for ctags regeneration to complete. Consider
   * increasing this value if ctags regeneration takes longer on your machine.
   *
   * Required.
   */
  ctagsSubProcessTimeoutMs: 10_000,
};
